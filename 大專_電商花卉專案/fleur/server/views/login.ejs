<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Fleur後臺系統</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            list-style: none;
            text-decoration: none;
        }

        body {
            height: 100vh;
            max-width: 1440px;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .loginForm {
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .loginForm * {
            font-size: 2rem;
        }

        .loginForm__iptBox {}

        .loginForm__ipt {}

        .loginForm__submitBtn {
            margin-left: auto;
        }
    </style>
</head>

<body>
    <form action="/backstage/login" method="post" class="loginForm">
        <p class="loginForm__iptBox">帳號：<input type="text" name="acc" class="loginForm__ipt"></p>
        <p class="loginForm__iptBox">密碼：<input type="password" name="pwd" class="loginForm__ipt"></p>
        <input type="submit" value="登入" class="loginForm__submitBtn">
    </form>
</body>

</html>